import type {Metadata} from 'next';import {notFound} from 'next/navigation';import {ReaderView} from '@/features/reader/reader-view';import {getMockChapter,getMockChapters,getMockManga,getMockPages} from '@/lib/mock-data';import {getWeebCentralChapterContext} from '@/sources/weebcentral/weebcentral-source';
async function resolve(chapterId:string){if(chapterId.startsWith('wc-')){try{return await getWeebCentralChapterContext(chapterId)}catch{return null}}const chapter=getMockChapter(chapterId);if(!chapter)return null;const manga=getMockManga(chapter.mangaId);if(!manga)return null;return {manga,chapter,chapters:getMockChapters(manga.id),pages:getMockPages(chapter.id)}}
export async function generateMetadata({params}:{params:Promise<{chapterId:string}>}):Promise<Metadata>{const {chapterId}=await params;const d=await resolve(chapterId);return {title:d?`${d.manga.title} · ${d.chapter.title}`:'Reader'}}
export default async function ReaderPage({params}:{params:Promise<{chapterId:string}>}){const {chapterId}=await params;const d=await resolve(chapterId);if(!d)notFound();return <ReaderView manga={d.manga} chapter={d.chapter} chapters={d.chapters} pages={d.pages}/>}
