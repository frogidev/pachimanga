"use client";
import { useEffect,useState } from "react";import { getMyAnimeListToken,isMyAnimeListConfigured,syncMyAnimeListEntry } from "@/lib/tracking/myanimelist";import { getTrackerLink,removeTrackerLink,saveTrackerLink,type TrackerLink } from "@/lib/tracking/tracker-links";import type { LibraryReadingStatus } from "@/lib/library/library-state";import type { Manga } from "@/types/models";
type Result={id:number;title:string;coverUrl:string;chapters:number|null};
export function MyAnimeListTrackingPanel({manga,progress,status}:{manga:Manga;progress:number;status:LibraryReadingStatus}){const[connected,setConnected]=useState(false);const[link,setLink]=useState<TrackerLink|null>(null);const[results,setResults]=useState<Result[]>([]);const[busy,setBusy]=useState(false);const[message,setMessage]=useState("");const configured=isMyAnimeListConfigured();useEffect(()=>{let cancelled=false;if(!configured)return;void Promise.all([getMyAnimeListToken(),getTrackerLink(manga.sourceId,manga.id,'myanimelist')]).then(([token,current])=>{if(!cancelled){setConnected(Boolean(token));setLink(current)}}).catch(()=>{});return()=>{cancelled=true}},[configured,manga.id,manga.sourceId]);if(!configured||!connected)return null;async function search(){setBusy(true);try{const response=await fetch(`/api/tracking/myanimelist/search?q=${encodeURIComponent(manga.title)}`,{cache:"no-store"});const body=await response.json() as {items?:Result[];error?:string};if(!response.ok)throw new Error(body.error||"MyAnimeList search failed.");setResults(body.items||[])}catch(error){setMessage(error instanceof Error?error.message:"MyAnimeList search failed.")}finally{setBusy(false)}}async function choose(item:Result){setBusy(true);try{await saveTrackerLink({provider:'myanimelist',sourceId:manga.sourceId,mangaId:manga.id,mediaId:String(item.id),mediaTitle:item.title});setLink({provider:'myanimelist',sourceId:manga.sourceId,mangaId:manga.id,mediaId:String(item.id),mediaTitle:item.title,updatedAt:new Date().toISOString()});setResults([]);setMessage(`Linked to MyAnimeList: ${item.title}.`)}catch(error){setMessage(error instanceof Error?error.message:"Could not save tracker link.")}finally{setBusy(false)}}async function sync(){if(!link)return;setBusy(true);try{await syncMyAnimeListEntry({mediaId:Number(link.mediaId),progress,status});setMessage(`MyAnimeList synced through chapter ${Math.floor(progress)}.`)}catch(error){setMessage(error instanceof Error?error.message:"MyAnimeList sync failed.")}finally{setBusy(false)}}async function unlink(){if(!link)return;setBusy(true);try{await removeTrackerLink(manga.sourceId,manga.id,'myanimelist');setLink(null);setMessage("MyAnimeList title link removed.")}finally{setBusy(false)}}return <div className="mt-3 rounded-xl border border-white/[.07] bg-white/[.025] p-3"><div className="flex flex-wrap items-center justify-between gap-3"><div><div className="text-sm font-medium text-zinc-200">MyAnimeList tracking</div><div className="mt-1 text-xs text-zinc-500">{link?`Linked to ${link.mediaTitle}`:"Connect this manga to its MyAnimeList entry."}</div></div><div className="flex gap-2">{link?<><button type="button" disabled={busy} onClick={()=>void sync()} className="button-secondary px-3 py-2 text-xs disabled:opacity-40">Sync now</button><button type="button" disabled={busy} onClick={()=>void unlink()} className="rounded-xl px-3 py-2 text-xs text-zinc-500">Unlink</button></>:<button type="button" disabled={busy} onClick={()=>void search()} className="button-secondary px-3 py-2 text-xs disabled:opacity-40">{busy?"Searching…":"Find on MAL"}</button>}</div></div>{results.length?<div className="mt-3 grid gap-2">{results.map(item=><button key={item.id} type="button" disabled={busy} onClick={()=>void choose(item)} className="flex min-h-11 items-center justify-between gap-3 rounded-xl border border-white/[.06] px-3 py-2 text-left text-xs hover:bg-white/[.04]"><span className="truncate text-zinc-300">{item.title}</span><span className="text-zinc-600">#{item.id}</span></button>)}</div>:null}<p className="mt-2 min-h-4 text-xs text-zinc-500" role="status">{message}</p></div>}
